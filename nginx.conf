  GNU nano 6.2                                           nginx.conf                                                     server {
    listen 443 ssl;
    server_name electrosha20.com.ar www.electrosha20.com.ar;

    ssl_certificate /etc/letsencrypt/live/electrosha20.com.ar/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/electrosha20.com.ar/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    # Proxy para el frontend
    location / {
        proxy_pass http://vps-4827345-x.dattaweb.com:3000;  # Redirige a tu contenedor frontend
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Proxy para el backend (API)
    location /api/ {
        proxy_pass http://vps-4827345-x.dattaweb.com:8080;  # Redirige a tu backend
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Proxy inverso para MinIO
    location /webapp/ {
        proxy_pass http://vps-4827345-x.dattaweb.com:9000;  # Endpoint real del servidor MinIO
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}

server {
    listen 80;
    server_name electrosha20.com.ar www.electrosha20.com.ar;

    # Redirigir tráfico HTTP a HTTPS
    return 301 https://$host$request_uri;
}

